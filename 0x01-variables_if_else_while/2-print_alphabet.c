#include <stdio.h>
/**
 *main - Entry point
 *Description: 'Print alphabets loercase
 *Return: Always 0
 */
int main(void)
{
	for (char c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	putchar('\n');

	return (0);
}
