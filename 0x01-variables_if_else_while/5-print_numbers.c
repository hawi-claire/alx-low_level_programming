#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print single digit numbers of base ten'
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 0; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
