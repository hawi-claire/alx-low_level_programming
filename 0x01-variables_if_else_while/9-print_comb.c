#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print comb of numbers'
 * Return: always 0
 */
int main(void)
{
	int m:
		for (n = 48; m <= 57; n++)
		{
			putchar(n);
			if (n == 57)
			{
				continue;
			}
			putchar(',');
			putchar(' ');
		}
	putchar('\n');
	return (0);
}
