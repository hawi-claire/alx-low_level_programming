#include "main.h"
                    	
/**
 * _isdigit - checks for a digit from 0 through 9
 * c is the integer to be checked
 * If c is a digit, return 1, else 0
 */
int _isdigit(int c)
{
        	return (c >= '0' && c <= '9');
}
